<%= form_for @form, url: song_path(@form.model), method: :put do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :artist %>
  <%= f.text_field :artist %>

  <%= f.submit "Update" %>
<% end %>